//! Workspace-level configuration, system prompt building and in-memory
//! session batch records for the ManualAid CLI loop.
//! 面向 ManualAid CLI loop 的工作区配置、系统提示词构建与内存会话批次记录。

pub mod config;
pub mod context;
pub mod prompt;
pub mod session;
